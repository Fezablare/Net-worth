# Required in production. Default local PIN is 1234 when unset.
NET_WORTH_PIN=1234

# Set automatically when you create a Vercel Blob store in the project.
# Local dev uses data/net-worth.json instead — leave this unset locally.
# BLOB_READ_WRITE_TOKEN=
